/target
*.rs.bk
*.rlib
*.prof*
.idea/
.vscode/
*.log
.env*

# Unreviewed insta snapshots — don't commit until reviewed.
tests/snapshots/*.snap.new

# M04: generated trace assets (regenerated by `cargo run --bin gen_traces`).
web/traces/

# M04: trunk build output.
web/dist/
