//! M08.2 — randomized (seeded) scheduler tests.
//!
//! Stub during Phase 2 — concrete tests land in Phase 3 (T020, T021) and
//! Phase 4 (T030) and Phase 6 (T040). This placeholder just ensures the
//! test target compiles before the scheduler implementation lands.

#[test]
fn placeholder() {
    // Replaced by `same_seed_determinism` / `single_thread_invariance` /
    // `different_seed_divergence` / `deadlock_detection` in later phases.
}
